// Test file

func divide(args: [float: 2]): float {
    if (args[0] == 0.0 || args[1] == 0.0) {
        return 0;
    }

    args[0] / args[1]
}

func main() {
    let mut args: [float: 2] = [0.0, 1.0];
    let mut result = 0.0;

    result = divide(args);
}