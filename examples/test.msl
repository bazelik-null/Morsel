// Test file

func divide(x: int, y: int): float {
    if (x == 0 || y == 0) {
        return 0;
    }

    x / y
}

func main() {
    let x = 1;
    let y = 2;
    let mut z: float = 0;

    z = divide(x, y);
}